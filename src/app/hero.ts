/*
*
* Interface provides the structure of a hero object but not the implementation
* Only used to define the shape of the object for developers
* Not used by Angular at runtime, only for type checking
* Removed when transpiled to JavaScript
*/

export interface Hero {
  id: number;
  name: string;
}
